
// For Windows/MAC machine
//Generate the keys first on the device
ssh-keygen -t ed25519 -C "your_email@example.com"

//Check if your git is configured
git config --list

git config --global user.name “myusername”
git config --global user.email username@usermail.com

$ ssh-add <path/to/key>
$ ssh-add /c/Users/location/.ssh/id_rsa

eval $(ssh-agent)       // For Windows

eval "$(ssh-agent -s)" // For MAC

Use git 2.28 if others dont work


git remote add origin git@github.com:$USER/your-repository.git

git remote set-url origin git@github.com:username/your-repository.git

//================================================================
Git on Linux 

ssh-keygen -t ed25519

sudo apt-get install git

sudo apt install libcurl4-gnutls-dev libexpat1-dev gettext libz-dev libssl-dev build-essential

git config --global user.name "****"
git config --global user.email "****"
git config --list
git config --global --list


ssh -vvv -p 29418 git@github.com

git remote add origin git@github.com:$USER/your-repository.git
git remote set-url origin git@github.com:username/your-repository.git

git push -u origin main




git init

git remote add origin "https://dev.azure.com/<org>/<project>/_git/<repo>"


git add .
git commit -m "Initial commit for veboxdocker"



git branch -M main
git push -u origin main

git pull origin main --allow-unrelated-histories

git add README.md


git branch --show-current



Generating the private the 
ssh-keygen -t rsa -b 4096 -f "$env:USERPROFILE\.ssh\azure_devops_key"

checking of created 
Get-ChildItem "$env:USERPROFILE\.ssh\azure_devops_key*"




BpGPVfoWyhoiCMW5FSsT0oakEKOBjYtLtnDL9h69ccV3uta6j3qAJQQJ99BKACAAAAAfzhW0AAASAZDO3LEM


git commit --amend --reset-author

git remote remove origin


git reset --hard origin/master
































// For Windows/MAC machine
//Generate the keys first on the device
ssh-keygen -t ed25519 -C "your_email@example.com"

//Check if your git is configured
git config --list

git config --global user.name “myusername”
git config --global user.email username@usermail.com

$ ssh-add <path/to/key>
$ ssh-add /c/Users/location/.ssh/id_rsa

eval $(ssh-agent)       // For Windows

eval "$(ssh-agent -s)" // For MAC

Use git 2.28 if others dont work


git remote add origin git@github.com:$USER/your-repository.git

git remote set-url origin git@github.com:username/your-repository.git

//================================================================
Git on Linux 

ssh-keygen -t ed25519

sudo apt-get install git

sudo apt install libcurl4-gnutls-dev libexpat1-dev gettext libz-dev libssl-dev build-essential

git config --global user.name "****"
git config --global user.email "****"
git config --list
git config --global --list


ssh -vvv -p 29418 git@github.com

git remote add origin git@github.com:$USER/your-repository.git
git remote set-url origin git@github.com:username/your-repository.git

git push -u origin main

git log --oneline --decorate --graph master --max-count=5



#steps:
  #- task: GitVersion@5
    #displayName: GitVersion
    #inputs:
      #runtime: full

  - script: 'wget http://www-eu.apache.org/dist/maven/maven-3/3.8.6/binaries/apache-maven-3.8.6-bin.zip'
